export interface Dog {
	id: string;
	breed: string;
	name: string;
	age: number;
	img: string; 
  }
  
  export interface Location {
	zipCode: string;
	city: string;
	state: string;
  }
  
  export interface Match {
	match: string;
  }
  